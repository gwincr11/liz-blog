<!DOCTYPE html>
<html>
<head>
  <title>Liz Foster-Shaner | <%= yield(:title) %></title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
	<header>
		<h1><%= link_to "Liz Foster-Shaner", root_path %></h1>
		<nav>
			<ul>
				<li><%= link_to "Blog", root_path %></li>
				<% Category.all.each do |cat| %>
					<li><%= link_to cat.title, portfolio_category_path(cat) %></li>
				<% end %>
			</ul>
		</nav>
	</header>
	<%= yield %>

	<%= debug(params) if Rails.env.development? %>
</div>
</body>
</html>
